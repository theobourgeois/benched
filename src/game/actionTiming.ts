/** Charged slap-shot downswing from the loaded pose to puck contact. Quick shots have no delay. */
export const SHOT_DOWNSWING = 0.075;
/** Save contact, hold and recovery, sampled by both the live renderer and replay. */
export const GOALIE_SAVE_TIME = 0.7;
